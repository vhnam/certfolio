---
title: Certfolio
description: A centralized vault for IxDF learning notes, comprehensive certifications, and master class insights
template: splash
hero:
  tagline: Curated insights, notes, and projects from my <strong>IxDF learning path</strong> and beyond — a centralized vault for continuous growth
---

import { CardGrid, LinkCard } from "@astrojs/starlight/components";
import { masterClasses } from "@/data/master-classes.json";
import { certificates } from "@/data/certificates.json";
import CertificateItem from "@/components/user-components/CertificateItem.astro";

<section class="py-16">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <div class="mb-10 text-center">
      ## Certificates
      Deep-dive courses with comprehensive learning notes
    </div>

<div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
  {certificates.map((certificate) => (
    <CertificateItem key={certificate.title} certificate={certificate} />
  ))}
</div>

  </div>
</section>

<section class="border-t border-border/40 bg-gradient-to-b from-secondary/10 to-background py-20">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8 max-w-5xl">
    <div class="mb-12 text-center">
      ## Master Classes
      Curated insights from 90-120 minute webinars — distilled into quick reads
    </div>

    <CardGrid>
      {masterClasses.map((masterClass) => (
        <LinkCard
          key={masterClass.title}
          title={masterClass.title}
          href={masterClass.link}
          description={masterClass.description}
        />
      ))}
    </CardGrid>

  </div>
</section>
